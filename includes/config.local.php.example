<?php

declare(strict_types=1);

/**
 * Copy to config.local.php (do not commit) and set a bcrypt hash:
 *
 *   php -r 'echo password_hash("your-secret-password", PASSWORD_DEFAULT), "\n";'
 */
// const ADMIN_PASSWORD_HASH = '$2y$10$...';

/**
 * Optional: receive contact form submissions without making your email public.
 * Example:
 *   const CONTACT_FORM_TO_EMAIL = 'you@example.com';
 */
// const CONTACT_FORM_TO_EMAIL = 'you@example.com';

/**
 * Optional: force a specific From email (recommended for deliverability).
 */
// const CONTACT_FORM_FROM_EMAIL = 'no-reply@yourdomain.com';

/**
 * Optional SMTP transport (recommended if local mail() is unavailable).
 * Example Gmail: host smtp.gmail.com, port 587, encryption tls.
 */
// const CONTACT_SMTP_HOST = 'smtp.gmail.com';
// const CONTACT_SMTP_PORT = 587;
// const CONTACT_SMTP_USERNAME = 'you@gmail.com';
// const CONTACT_SMTP_PASSWORD = 'app-password';
// const CONTACT_SMTP_ENCRYPTION = 'tls'; // tls | ssl | ''
